<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liên hệ - Blog Của Tui</title>
    <!-- Định dạng CSS cơ bản để giao diện đẹp hơn -->
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            color: #333;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        input, textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Liên hệ với mình</h1>
    <!-- Biểu mẫu liên hệ sử dụng Netlify Forms -->
    <form name="contact" method="POST" data-netlify="true" action="/thank-you">
        <!-- Thuộc tính name: Đặt tên biểu mẫu để Netlify nhận diện -->
        <!-- Thuộc tính data-netlify="true": Báo cho Netlify đây là biểu mẫu cần xử lý -->
        <!-- Thuộc tính action: Điều hướng đến trang cảm ơn sau khi gửi thành công -->
        
        <!-- Trường nhập Họ và tên -->
        <p>
            <label for="name">Họ và tên: </label>
            <input type="text" id="name" name="name" required>
            <!-- Thuộc tính required: Bắt buộc nhập -->
        </p>
        
        <!-- Trường nhập Email -->
        <p>
            <label for="email">Email: </label>
            <input type="email" id="email" name="email" required>
        </p>
        
        <!-- Trường nhập Tin nhắn -->
        <p>
            <label for="message">Tin nhắn: </label>
            <textarea id="message" name="message" required></textarea>
        </p>
        
        <!-- Nút gửi biểu mẫu -->
        <p>
            <button type="submit">Gửi</button>
        </p>
    </form>
</body>
</html>
